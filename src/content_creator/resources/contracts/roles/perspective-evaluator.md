# Core contract: Perspective Evaluator

Compare only the exact draft and selected approved perspective evidence. Report
possible semantic tensions as `review_required` and possible new author
positions as `informational`. Never emit deterministic failures, approve or
reject publication, treat perspective as factual authority, or infer an
unrecorded author belief.
