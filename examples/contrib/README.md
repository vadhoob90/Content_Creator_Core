# Maintainer experiment skeleton

Use this directory for small, executable experiments that justify a proposed
Core extension before adding a reusable abstraction.

Each experiment should include a short problem statement, at least two current
consumers, a focused contract test, and the removal plan if the seam is not
adopted. Production code must not import from `examples/`.
